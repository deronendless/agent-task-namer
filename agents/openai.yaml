interface:
  display_name: "Codex Task Namer"
  short_description: "Name and organize Codex tasks in the user's language"
  default_prompt: "Use $codex-task-namer to name this task as emoji Type | YYMMDD | Topic, following the language of its main user request."

policy:
  allow_implicit_invocation: true
