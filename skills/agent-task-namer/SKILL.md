---
name: agent-task-namer
description: "Name Codex tasks as emoji Type | YYMMDD | Topic in the user's language. Use for explicit naming requests, trusted local naming reminders, and explicitly requested batches or restoration; previews do not rename tasks."
---

# Agent Task Namer

Read the [shared naming workflow](../../SKILL.md) before reading or changing task titles. It contains the client operations, language and creation-date rules, and authorization boundaries. Resolve its references and scripts relative to that shared file's directory.

This entry is bundled with the Codex plugin. For installation or automatic-trigger troubleshooting, read the [automatic naming setup](../../references/automatic.md). Installing this plugin does not itself authorize changes to existing task titles.
